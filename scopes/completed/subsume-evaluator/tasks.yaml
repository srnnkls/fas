---
scope: subsume-evaluator
created: 2026-04-22
status: completed
completed: 2026-04-22
notes: >
  T3 scope narrowed during implementation. Subsume checks the pattern as a
  static shape; it does not substitute input values into `when`, so sibling
  refs, let bindings, input-dependent `if` clauses, and computed hidden
  count fields do not work as the scope originally claimed. List element
  patterns do. The load-time lint (T4) rejects unbound identifiers that
  would silently misbehave; the AGENTS.md Authoring Rules section (T6)
  documents the supported vocabulary and the alternatives.

  Two upstream CUE bugs surfaced and were fixed on a local fork
  (../cue, branch fix/subsume-validator-env-and-bool-bounds) pinned via
  go.mod replace:
    - BuiltinValidator nil-deref when args carry lazy refs (let, hidden).
    - BoundExpr.evaluate rejecting != / == bounds on BoolKind.
---

tasks:
  - id: T1
    subject: Replace inputSatisfies with Subsume
    description: >
      Delete `inputSatisfies` in `internal/evaluator/evaluator.go:56-87`. Replace
      the match call in `Evaluate` with `rule.When.Subsume(input) == nil`. Keep
      `checkWhen` (structural validation of the rule's `when` clause). No input
      binding — the pattern is self-describing; sibling references, `let`,
      `for`, `if`, and list patterns inside `when` all reference the pattern's
      own structure.
    files:
      - internal/evaluator/evaluator.go
      - internal/evaluator/evaluator_test.go
    status: completed
    test_hints:
      - All 14 existing evaluator tests pass unchanged.
      - New test: `when: {tool_name: "Bash"}` + input missing tool_name → no match.
      - New test: `when: {command: =~"^rm "}` + input with matching command → match.
      - New test: optional field `when: {flags?: force?: !=true}` semantics preserved.
      - Evaluator function body is a single Subsume call per rule — no recursion.

  - id: T2
    subject: Confirm struct-level | via subsumption
    description: >
      No code change — subsumption already handles struct-level disjunction
      correctly. Add test coverage confirming the behavior and document that
      raw `|` is the idiomatic shape disjunction (no custom operator required).
    files:
      - internal/evaluator/evaluator_test.go
    status: completed
    depends_on: [T1]
    test_hints:
      - `when: {tool_name: "Bash"} | {tool_name: "Write"}` + Bash input → match.
      - Same `when` + Write input → match.
      - Same `when` + Read input → no match.
      - Nested: `{tool_name: "Bash", tool_input: {command: =~"^rm"} | {command: =~"^dd"}}` works.

  - id: T3
    subject: Test coverage for sibling refs, let, if, and list patterns
    description: >
      Add evaluator tests that exercise CUE's native features inside `when`
      without any binding: sibling references (`command: targets[0]`),
      `let` (`let cmd = tool_input.command`), `if` clauses referencing siblings
      (`if list.Contains(flags, "--force") { command: =~"^git push" }`),
      and list patterns (`targets: [...=~"^/etc/"]`). Confirms that
      subsumption + CUE native grammar covers the author vocabulary.
    files:
      - internal/evaluator/evaluator_test.go
    status: completed
    depends_on: [T1]
    test_hints:
      - `if` inside struct gates fields based on sibling value.
      - `let` inside `when` references a sibling field.
      - List pattern `[...=~"^/etc/"]` requires every element to match.
      - Computed hidden field `_n: len(flags)` + `_n: >5` enforces count.

  - id: T4
    subject: Load-time lint for rejected constructs in when
    description: >
      New file `internal/config/lint.go`. At load time (after compileRuleFile,
      before rule extraction), AST-walk each rule's `when` subtree. Three
      rejections:
        - Cross-rule refs: any SelectorExpr whose root is another top-level rule
          field's `when`/`then`/`meta`.
        - Self-refs into then/meta: SelectorExpr touching the same rule's
          `then` or `meta` from within `when`.
        - Unbound identifiers: any Ident not resolvable as a stdlib import or
          a local hidden sibling (`_foo`).
      Emit plain Go errors (wrapped via ruleLoadError) for now; the `diag-errors`
      scope migrates these to the unified diagnostic renderer.
    files:
      - internal/config/lint.go
      - internal/config/loader.go
      - internal/config/loader_test.go
    status: completed
    depends_on: [T1]
    test_hints:
      - Rule with `when: other_rule.when.tool_name` → load error names both rules.
      - Rule with `when: {x: self.then.rule_id}` → load error "ref to then/meta".
      - Rule with `when: {x: foo}` where foo is unbound → load error names foo.
      - Rule with `when: {x: _local}` where `_local` exists → loads fine.
      - Rule with `when: {x: bash.#SafeCommand}` with stdlib import → loads fine.
      - Rule with sibling ref `{command: targets[0], targets: [...]}` → loads fine.

  - id: T5
    subject: Scrut coverage for sibling-based vocabulary
    description: >
      Audit `tests/policies/*.cue`. Where a fixture can be expressed more
      clearly using sibling refs, `let`, `if`, list patterns, or struct-level `|`,
      migrate. Keep rule IDs and observable behavior unchanged. Add 2-3 scrut
      blocks exercising sibling-ref patterns and struct-level disjunction.
    files:
      - tests/policies/*.cue
      - tests/policies.md
    status: completed
    depends_on: [T2, T3]
    test_hints:
      - `scrut test tests/policies.md` succeeds for all blocks (existing + new).
      - Go test suite remains green.

  - id: T6
    subject: Document subsumption model in AGENTS.md
    description: >
      Add an "Authoring rules" section to `AGENTS.md` covering: (a) the
      rule's `when` IS the pattern the input must fit; (b) subsumption is
      the evaluation primitive; (c) structural negation via De Morgan +
      CUE leaves; (d) sibling refs / let / if / list patterns replace any
      need for a binding name; (e) what the lint rejects. Short — one page.
    files:
      - AGENTS.md
      - scopes/draft/quae/design.md
    status: completed
    depends_on: [T1, T4]
    test_hints:
      - AGENTS.md has a section titled "Authoring rules".
      - De Morgan example table present (not (a=1 AND b=2) → {a: !=1} | {b: !=2}).
      - "Rule is pattern, not predicate" framing documented.
